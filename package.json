{
  "name": "cardstock",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter geom-wasm build && pnpm --filter web build",
    "build:web": "pnpm --filter web build",
    "build:wasm": "pnpm --filter geom-wasm build",
    "lint": "pnpm --filter web lint",
    "preview": "pnpm --filter web preview"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "@types/node": "^24.6.0",
    "eslint": "^9.36.0",
    "globals": "^16.4.0",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.45.0",
    "wasm-pack": "^0.13.0"
  }
}
